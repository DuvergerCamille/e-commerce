{% extends 'layoutAdmin.html.twig' %}

{% block title %}

Profil Client- {{ parent() }}

{% endblock %}

{% block body %}

<div class="flash">
    {% for message in app.flashes('notice') %}
        <p>{{ message }}</p>
    {% endfor %}
</div>

<h2>Edition du profil client</h2>

{{ include("user/_form.html.twig") }}

{% endblock %}