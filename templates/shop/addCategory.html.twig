{% extends 'layoutAdmin.html.twig' %}

{% block title %}

Ajout - {{ parent() }}

{% endblock %}

{% block body %}

<div>
    {% for message in app.flashes('notice') %}
        <p>{{ message }}</p>
    {% endfor %}
</div>

<h2>Ajout d'une catégorie</h2>

<div>
    {{ form(form) }}
</div>

{% endblock %}