{% extends 'layout.html.twig' %}

{% block body %}

<div class="flash">
    {% for message in app.flashes('notice') %}
        <p>{{ message }}</p>
    {% endfor %}
</div>

    {% for instru in listInstruments %}
    <li class="article">
        {{ instru.name }} Prix: {{ instru.price }}$
        <a href="{{ path('achat', {'name': 'instrument', 'id': instru.id}) }}">
        <button type="button">
        Acheter
        </button>
        </a>
    </li>
    {% else %}
    <li class="article">Pas d'instrument dans cette catégorie.</li>
    {% endfor %}
{% endblock %}
