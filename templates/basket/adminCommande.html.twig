{% extends "layoutAdmin.html.twig" %}

{% block title %}
    Commandes - {{ parent() }}
{% endblock %}

{% block body %}
<div class="flash">
    {% for message in app.flashes('notice') %}
        <p>{{ message }}</p>
    {% endfor %}
</div>

<h2>Commandes</h2>

{% for cmd in listCommandes %}
<li class="article">
    Commande n°{{ cmd.id }} 
    <a href="{{ path('supprimer_commande', {'id': cmd.id}) }}">
      <button type="button">
      Supprimer
      </button>
    </a>
    <a href="{{ path('voir_commande', {'id': cmd.id}) }}">
      <button type="button">
      Voir
      </button>
    </a>
</li>
{% else %}
<li class="article">Pas de commande pour le moment.</li>
{% endfor %}
{% endblock %}